# Legacy path: `videos/user-guide/`

**Displayed product name:** User Manual (not “User Guide”).

This folder path (`/videos/user-guide/`, locale variants `user-guide-es/`, etc., and
`pocketbudjet-user-guide.mp4`) is kept for stable URLs, redirects, and sitemap
entries. Renaming the folder would break many chapter deep-links and Quiltt/partner
bookmarks into the interactive deck.

When writing user-facing copy, use **User Manual** / **user manual**.
Do not call this the Walkthrough — that is `videos/pocketbudjet/partner-showcase/`.

Canonical written PDF: `/docs/pocketbudjet/PocketBudJet_UserManual.pdf`
(legacy alias: `PocketBudJet_UserGuide.pdf`).
